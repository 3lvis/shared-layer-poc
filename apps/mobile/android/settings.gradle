pluginManagement {
  repositories {
    gradlePluginPortal()
    google()
    mavenCentral()
  }
  // Resolve React Native Gradle plugin from hoisted workspace node_modules
  includeBuild("../../../node_modules/@react-native/gradle-plugin")
}

plugins {
  id("com.facebook.react.settings")
}

// Generate autolinking.json and include native modules discovered by the RN CLI
reactSettings.autolinkLibrariesFromCommand()

rootProject.name = 'mobile'
include(":app")
