plugins {
    id("com.facebook.react.rootproject")
}

ext {
    buildToolsVersion = "35.0.0"
    minSdkVersion = 24
    compileSdkVersion = 34
    targetSdkVersion = 34
    ndkVersion = "26.1.10909125"
    kotlinVersion = "1.9.22"
}

allprojects {
    repositories {
        google()
        mavenCentral()
        // Local Maven repo shipped with Detox for the Android AAR
        // Use property assignment syntax to avoid Gradle deprecation warnings
        maven { url = uri("$rootDir/../../../node_modules/detox/Detox-android") }
    }
}
